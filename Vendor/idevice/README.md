# idevice FFI dependency

The large `idevice.h` and `libidevice_ffi.a` files are intentionally not bundled in this archive.
Run `Scripts/bootstrap_vendor.sh` before building. The script downloads the exact files from the pinned Locus upstream commit noted in `THIRD_PARTY_NOTICES.md`.
