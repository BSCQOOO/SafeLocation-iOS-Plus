# Safe Location 1.8.0

## Apple 地图风格与定位稳定性重构

- 地图个人位置改为直接使用 MapKit 原生 UserAnnotation，使用系统蓝点、精度表现和设备方向显示，不再自行绘制模拟定位箭头。
- 新增地图左侧双浮动控件：朝北复位 + 2D/3D 切换；浮动按钮使用独立高优先级命中，避免触发底层地图选点。
- 3D 模式改为 MapCamera pitch 驱动，切换时保留当前中心、缩放和朝向，地图立体效果与系统 MapKit 行为一致。
- 定位按钮优先精确回到 Safe Location 当前模拟坐标；恢复真实定位后只接受新的非 simulated Core Location fix，避免短时间误用旧的模拟坐标。
- MapLocationProvider 记录 Core Location 的 simulated source、heading、course，并使用 BestForNavigation 精度。
- BackgroundLocationKeeper 升级为 BestForNavigation，并同步采集 heading/course。
- 控制抽屉背景改为 iOS 26+ 原生 Liquid Glass 背景，旧系统回退 ultraThinMaterial，降低黑色实心感。
- 抽屉展开时搜索框改用系统 Safe Area 间距，不再使用屏幕绝对位置。
- 恢复真实定位改为幂等操作：已经处于真实定位时再次点击不会生成 notActive 错误，也不会破坏界面状态。
- 主控制抽屉加入自动恢复机制，即使系统意外 dismiss 也会立即重新呈现，修复“恢复真实定位后只剩定位按钮”的问题。

# Safe Location 1.7.0

## Apple 地图式抽屉第二轮真机修复

- 一级抽屉高度压缩为仅容纳拖拽条与搜索框，不再露出位置状态或功能区。
- 去掉自定义 Presentation Corner Radius，直接使用系统原生 Sheet 圆角与档位过渡，让形态更接近 Apple 地图。
- 摇杆模式下如果用户手动缩放、旋转或拖动地图，会自动退出“镜头跟随”；之后推动摇杆只更新模拟位置，不再强行把地图镜头拉回。
- 地图点选改为只修改目标坐标，不再改变当前缩放、旋转、俯仰或可视区域。
- 定位按钮加入独立高优先级触摸拦截和地图点选抑制窗口，避免点击定位按钮同时触发底层地图选点。
- 收藏按钮现在会真正保存当前位置，并提供“已收藏”视觉反馈和成功触感。
- “最近位置 → 查看全部”改为显式打开完整位置列表，不再依赖静态标题文本。
- 保留三段系统原生 PresentationDetents：搜索档 / 中档 / 大档。

# Safe Location 1.6.0

## Apple 地图式三段抽屉重构

- 根据真机录屏重新设计主控制区，改用 SwiftUI 原生 PresentationDetents，不再维护自定义拖拽物理。
- 正常模式固定三档：底部搜索档 / 中档控制面板 / 全屏大档，拖拽、吸附和回弹交给系统处理，手感更接近 Apple 地图。
- 搜索框从地图顶部彻底移除，集中到抽屉顶部；最小档主要显示拖拽条和搜索框。
- 搜索获得焦点后自动升到大档，搜索结果在抽屉内部滚动；结果数量由 6 条增加到 12 条。
- 中档显示定位状态、Teleport / 恢复真实和四个圆形快捷入口，布局参考 Apple 地图“地点”快捷入口。
- 大档继续显示路线、VPN、设置、最近位置和连接状态。
- 地图定位按钮改为抽屉外侧右下方的浮动圆形按钮，位置随抽屉档位联动，视觉参考 Apple 地图右侧地图控制。
- 摇杆继续保持独立低矮模式，只显示摇杆本身和必要控制，尽量保留地图可视区域。
- 保留系统 UserAnnotation、模拟位置标记和真实/模拟位置回中逻辑。

# Safe Location 1.5.1

## 最终验收修正

- 清理 MapLocationProvider 的 actor-isolation 警告，避免未来 Swift 6 模式下把定位 delegate 警告升级为错误。
- 保持 1.5.0 的顶部搜索、低矮摇杆条、定位按钮修复和连续高度抽屉物理逻辑不变。

# Safe Location 1.5.0

## 真机反馈修复

- 搜索框改为真正的顶部 Safe Area Inset，不再依赖手工 top padding，固定在状态栏下方。
- 新增 MapLocationProvider，定位按钮优先回到模拟位置；未模拟时回到系统当前位置。
- 非模拟状态显示系统 UserAnnotation；模拟状态继续显示 Safe Location 自己的模拟位置标记。
- 摇杆模式改成独立的低矮底部控制条，只保留摇杆、速度、跟随/退出和回到位置，不再显示 Teleport、收藏、方案、定时等大面板。
- 进入摇杆模式后自动把地图镜头拉回模拟位置，摇杆移动时实时跟随，避免位置被功能区遮挡。
- 抽屉拖动逻辑彻底改写：拖动过程直接连续改变抽屉高度，不再移动整个抽屉；松手后根据 global 坐标系和 projected height 吸附到最近档位。
- 抽屉内容始终保持同一布局并由高度裁切，避免拖动过程中内容突然切换导致“抽搐”。
- 继续保持按钮区域与地图点选隔离。

# Safe Location 1.4.0

## 最终界面打磨

- 移除地图右上角系统定位按钮，改为抽屉右上角的圆形定位按钮，视觉和交互统一到控制面板。
- 搜索栏重新按顶部安全区定位，缩短与状态栏之间的空隙。
- 底部控制抽屉延伸到底部安全区，背景完整贴住屏幕底边，不再露出地图空缺。
- 抽屉拖动手势只绑定在顶部抓手区域，避免覆盖按钮点击区域，从结构上阻断按钮与地图点选冲突。
- 三档抽屉改为 interactive spring 动画，并利用 predictedEndTranslation 判断落点，提升跟手、阻尼和回弹感。
- 搜索结果继续使用独立可滚动列表，并显示滚动条；搜索模式下自动隐藏底部抽屉。
- 新增 UI 回归检查：禁止重新引入地图悬浮定位按钮、全抽屉 DragGesture 和底部安全区空缺相关实现。

# Safe Location 1.3.0

## 交互重构

- 搜索进入独立交互状态：键盘弹出后底部功能抽屉自动隐藏，给搜索结果留出更多空间。
- 搜索结果改为真正的可滚动列表，支持滑动浏览，并支持交互式收起键盘。
- 地图主界面改为类似 Apple 地图的底部抽屉：默认仅露出状态摘要，可向上滑到中档和完全展开。
- 底部抽屉是独立命中层，按钮和空白区域不再把点击穿透给底层地图。
- 中档显示 Teleport / 恢复真实 + 收藏 / 方案 / 定时；完全展开后再显示路线 / 摇杆 / VPN 和连接信息。
- 摇杆模式嵌入完全展开的底部抽屉，地图持续可见；模拟位置移动时默认自动跟随。
- 摇杆模式下禁用地图选点，避免拖动摇杆时误改目标位置。
- 新增“跟随中 / 自由查看”和“回到位置”。

# Safe Location 1.2.0

## 地图交互优化

- 搜索栏和底部控制区改用 Safe Area Inset，不再压住 MapKit 自带按钮。
- 底部按钮不再与地图点击手势共用同一覆盖区域，避免点“收藏 / 方案 / 定时 / 路线 / VPN”等按钮时误判成地图选点。
- 摇杆改为地图底部常驻控制台，不再弹出全屏 / Sheet。
- 摇杆移动时默认自动跟随模拟位置，地图上的“模拟位置”标记会实时移动。
- 新增“跟随中 / 自由查看”和“回到位置”控制，可随时查看周边地图。
- 摇杆模式下禁用地图点选，避免拖动摇杆或操作底栏时意外修改目标点。

# Safe Location 1.1.1

## 修复

- 修复 iOS 27 + LocalDevVPN 默认地址下的连接检测。
- LocalDevVPN 的本机 Tunnel IP 是 `10.7.1.1`，而 DVT 设备 / Peer IP 是 `10.7.0.1`；旧版错误地把 Peer IP 当作本机接口地址检查，导致 VPN 明明已连接却显示“未连接”。
- 诊断页现在同时显示本机 Tunnel IP 与设备 / Peer IP，方便排查。

# Safe Location 1.1.0

## 新增

- iOS 26+ 原生 Liquid Glass 界面，iOS 18–25 自动回退 Material。
- 位置方案中心：快捷城市卡片 + 自定义位置方案。
- 自定义方案可保存移动方式与自动恢复时间。
- 自动恢复倒计时：预设 15/30/60/120/240 分钟，支持 5–720 分钟自定义。
- Apple 地图 / Google 地图 / `geo:` / 嵌入坐标文本解析增强。
- 主界面剪贴板一键导入地图链接。
- App Intents：新增“导入地图位置”和“Teleport 到地图位置”，可组合成 Apple 地图共享表单快捷指令。
- `safelocation://timer?minutes=` 与 `safelocation://panic`。

## 优化

- 路线和摇杆移动不再每一步重复重建 resend / health Timer。
- 路线与摇杆连续更新时关闭重复成功触感，降低干扰和额外工作。
- 自动恢复截止时间写入 UserDefaults，回到前台时再次校验。
- GitHub Actions 自动选择 Runner 上最新 Xcode，以兼容 Liquid Glass SDK。
- 主界面工具区改成 3×2 紧凑布局。
- 状态卡显示自动恢复倒计时。

## 安全边界

- 不绕过 Apple 代码签名。
- 不隐藏 `isSimulatedBySoftware` 等系统模拟定位标记。
- 不提供反作弊、反风控规避。
